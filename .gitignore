*.local.md

# Node / Vite
node_modules
dist
dist-ssr
*.local
logs
*.log
pnpm-debug.log*

# Rust
target/
app/src-tauri/gen/schemas

# Tauri sidecar staging — written by scripts/build-sidecar.mjs, one binary per
# target triple, never project content.
app/src-tauri/binaries/

# The server bundle staged by release.yml before it is uploaded to the release.
dist-cli/

# Editor / OS
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
models/

# Python
__pycache__/
*.pyc

# Screenshots dropped in the repo root for review, never project content
ScreenShot_*.png

# Apple signing credentials — Developer ID .p12, App Store Connect .p8, and the
# passwords that unlock them. Never project content, and never publishable: the
# private key is what lets a build claim to be this developer.
apple/
